interface:
  display_name: "Remote Input Operator"
  short_description: "Reliable typing for remote Windows sessions"
  default_prompt: "Use $remote-input-operator to type and verify a CMD command in a remote Windows desktop."
policy:
  allow_implicit_invocation: true
